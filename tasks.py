# -*- coding: utf-8 -*-
"""
30 份安全分析"原始答案"——本实验的操纵对象。
约定：
- 全部手工构造，每份答案是一段朴素直白的分析陈述（这就是 Judge 眼里的"原始文风"）；
- 所有 IP 属于 RFC 5737 文档保留网段（192.0.2.0/24、198.51.100.0/24、203.0.113.0/24），
  域名一律 example.com/net/org，哈希为合成值，不指向任何真实资产；
- 事实必须可被正则提取：IPv4、CVE 编号、域名、端口上下文数字、风险等级；
  factcheck.py 依赖这一点做"改写前后事实集合必须相等"的硬校验。
"""

TASKS = [
    # ---------------- 漏洞分析 ----------------
    dict(id="va01", cat="漏洞分析", answer=(
        "该漏洞为Log4j2远程代码执行（CVE-2021-44228）。2.15.0版本之前的log4j-core受影响，"
        "攻击载荷通过HTTP头User-Agent字段传入，可在目标服务器执行任意命令。"
        "流量中观察到源IP 192.0.2.15对/app/api接口的探测，payload包含${jndi:ldap:}特征。"
        "建议升级log4j-core至2.17.1以上，并临时移除JndiLookup类。风险等级：高危。")),
    dict(id="va02", cat="漏洞分析", answer=(
        "目标主机3389端口对外开放且未安装2019年5月之后的系统更新，受CVE-2019-0708（BlueKeep）影响。"
        "该漏洞可在无需认证的情况下预认证远程代码执行，主要影响Windows 7与Server 2008 R2等旧系统，"
        "扫描确认主机系统版本为Windows Server 2008 R2 SP1且未启用NLA。"
        "建议安装对应累积更新，并在边界限制3389端口的来源访问。风险等级：严重。")),
    dict(id="va03", cat="漏洞分析", answer=(
        "WebLogic管理控制台存在未授权访问，结合CVE-2020-14882可绕过控制台认证，"
        "配合CVE-2020-14883实现远程代码执行。访问日志中发现攻击者请求"
        "/console/css/%252e%252e%252e%252e路径且返回200，源IP为198.51.100.40，利用已成功。"
        "建议安装2020年10月之后的官方补丁，临时方案为将控制台从外网下线。风险等级：严重。")),
    dict(id="va04", cat="漏洞分析", answer=(
        "Exchange服务器存在CVE-2021-26855（SSRF）与CVE-2021-26857（反序列化）的组合利用痕迹，"
        "IIS日志中出现大量针对/owa/auth/Current路径的异常POST请求，"
        "w3wp.exe进程下发现可疑的cmd子进程。判断为ProxyLogon利用链攻击，源IP为203.0.113.10。"
        "建议安装2021年3月累积更新，排查服务器上的webshell并轮换凭据。风险等级：严重。")),
    dict(id="va05", cat="漏洞分析", answer=(
        "应用采用Struts 2.3.31，受S2-045（CVE-2017-5638）影响，攻击者可通过Content-Type头"
        "注入OGNL表达式实现远程代码执行。WAF日志捕获到%{(#_='multipart/form-data')}特征串，"
        "来源IP为198.51.100.61。建议升级Struts至2.3.32或2.5.10.1以上版本，"
        "并在WAF封禁Content-Type中含OGNL特征的请求。风险等级：高危。")),

    # ---------------- 告警研判 ----------------
    dict(id="sa01", cat="告警研判", answer=(
        "告警显示主机10.0.0.8的22端口出现SSH密码爆破。源地址198.51.100.23在10分钟内产生412次"
        "认证失败，目标账号依次尝试root、admin、deploy，随后该源IP使用deploy账号认证成功一次。"
        "判断该主机已被攻破，建议立即隔离主机、重置deploy账号凭据并排查后续横向行为。"
        "风险等级：严重。")),
    dict(id="sa02", cat="告警研判", answer=(
        "告警显示文件上传接口被写入webshell。访问日志中，源IP 192.0.2.88于14:32向/upload.jsp"
        "提交名为avatar.jpg.jsp的文件，随后同一IP对/shell.jsp发起6次POST请求，响应码均为200，"
        "请求体中含cmd参数。判断webshell已成功落地并被访问，建议删除文件、封禁源IP并排查横向移动。"
        "风险等级：高危。")),
    dict(id="sa03", cat="告警研判", answer=(
        "主机10.0.0.51的进程pooling.exe持续连接矿池域名miner-pool.example.net，CPU占用95%。"
        "进程路径为C:\\Users\\public\\pooling.exe，无数字签名，创建时间为告警前1小时，"
        "外联目标解析至已知矿池地址段。判断为挖矿木马，建议隔离主机并排查计划任务持久化。"
        "风险等级：中危。")),
    dict(id="sa04", cat="告警研判", answer=(
        "SOC收到30条来自10.0.0.9的SSH认证失败告警。核实发现该主机为公司自动化巡检平台，"
        "昨日更新的专用巡检账号口令未同步，导致其按计划任务在凌晨3点巡检窗口批量登录失败，"
        "期间无其他来源IP参与。判断为配置问题导致的误报，建议更新巡检平台凭据并在告警平台加白。"
        "风险等级：低危。")),
    dict(id="sa05", cat="告警研判", answer=(
        "内网主机10.0.0.77对ns1.dnstun.example.com发起高频TXT查询，2小时内共1842条，"
        "查询子域名长度达48字符，base32编码特征明显，响应中携带长十六进制串。"
        "判断存在DNS隧道外传数据行为，建议封禁该域名、对该主机做全量取证并核查DLP策略。"
        "风险等级：高危。")),

    # ---------------- 代码审计 ----------------
    dict(id="ca01", cat="代码审计", answer=(
        "orders.php第37行将用户输入$_GET['id']未经过滤直接拼入SQL语句，第41行的mysqli_query"
        "执行了该拼接语句，存在报错注入漏洞，攻击者可通过union select读取任意表数据。"
        "建议改用参数化查询，并对id参数做整数类型强校验。风险等级：高危。")),
    dict(id="ca02", cat="代码审计", answer=(
        "AuditController.java第88行使用ObjectInputStream直接readObject用户Cookie中的base64数据，"
        "且工程classpath包含commons-collections 3.1，存在Java反序列化远程代码执行风险，"
        "攻击者可构造payload直接获得服务器权限。建议移除readObject入口、升级commons-collections"
        "至3.2.2以上并增加反序列化白名单。风险等级：严重。")),
    dict(id="ca03", cat="代码审计", answer=(
        "ReportExporter.java第132行将用户可控的format参数直接拼接到Runtime.exec的命令字符串中，"
        "未做任何校验，存在命令注入漏洞，攻击者可通过管道符注入获得反向shell。"
        "建议对format取值做白名单校验，并使用exec的数组参数形式避免shell解析。"
        "风险等级：严重。")),
    dict(id="ca04", cat="代码审计", answer=(
        "OrderQueryServlet第45行直接以请求参数orderId查询订单，未校验当前会话与订单的归属关系，"
        "存在水平越权（IDOR）。任意登录用户遍历orderId即可查看他人订单中的姓名、手机号和收货地址。"
        "建议在查询条件中加入会话归属校验，并将订单ID改为不可枚举的随机标识。风险等级：高危。")),
    dict(id="ca05", cat="代码审计", answer=(
        "UploadServlet第61行仅校验文件名后缀为jpg/png，未校验文件内容（magic number），"
        "文件名未重命名，且上传目录位于Web根目录下可直接执行，攻击者可上传jsp木马获取权限。"
        "建议将上传目录移出Web可执行路径、增加文件头校验并随机重命名。风险等级：高危。")),

    # ---------------- IOC 分析 ----------------
    dict(id="io01", cat="IOC分析", answer=(
        "从DNS日志中提取到主机10.0.0.66在24小时内查询的5个可疑域名：qxk2m4.example.com、"
        "t8zrwp.example.net、m3vd7q.example.com、h9p2xl.example.org、w6c4bn.example.net。"
        "共同特征为长度12~16字符、随机性高、TTL均为300、NXDOMAIN率超过80%，符合DGA特征。"
        "建议封禁上述域名并排查该主机上的可疑进程。风险等级：中危。")),
    dict(id="io02", cat="IOC分析", answer=(
        "本次事件共提取到3个攻击源IP：192.0.2.15（SSH爆破来源）、198.51.100.23（爆破成功来源）、"
        "203.0.113.77（勒索事件初始入口）。三者均出现在公开黑名单中，且活跃时间窗高度重合，"
        "判断为同一批控制资产。建议全部加入边界封禁名单并保留90天。风险等级：中危。")),
    dict(id="io03", cat="IOC分析", answer=(
        "三个样本的SHA256分别为9f2c4e7a1b8d5f3e6c0a9d8e7f6b5a4c3d2e1f0a9b8c7d6e5f4a3b2c1d0e9f8a、"
        "4c1a9e2b7d8f3a5c6e0b9d1f2a3c4e5b6f7a8b9c0d1e2f3a4b5c6d7e8f9a0b1c、"
        "8d3f6a2c5b9e1d4a7c0f3b6e9d2a5c8b1e4f7a0c3d6b9e2f5a8c1d4b7e0f3a68。"
        "导入表均包含VirtualAlloc与WriteProcessMemory，字符串中出现相同互斥量名Global\\finmk，"
        "判断为同一勒索家族的不同构建版本，建议按上述特征更新YARA规则。风险等级：中危。")),
    dict(id="io04", cat="IOC分析", answer=(
        "监测到针对我司的钓鱼基础设施：mail-secure-login.example.com与o365-verify.example.net，"
        "均使用Let's Encrypt证书、注册时间在3天内，页面仿冒统一登录门户，"
        "前者的A记录解析至192.0.2.200。判断为定向钓鱼活动，建议封禁上述域名与IP、"
        "在邮件网关拦截相关邮件并通知全员。风险等级：高危。")),
    dict(id="io05", cat="IOC分析", answer=(
        "受控主机10.0.0.51每小时整点后7分钟向198.51.100.250的8443端口发起TLS连接，"
        "JA3指纹与内部C2指纹库匹配，流量呈每5分钟一次的固定心跳。判断该IP为C2服务器，"
        "建议封禁该IP并回溯90天内与其通信过的全部主机。风险等级：高危。")),

    # ---------------- 风险评级 ----------------
    dict(id="rr01", cat="风险评级", answer=(
        "互联网暴露面上存在一台运行Apache Struts 2.5.30的历史服务器，受S2-057（CVE-2018-11776）影响，"
        "且该服务器可直达内网核心数据库网段。利用代码公开、利用复杂度低、资产重要性高，"
        "综合评级为严重。建议立即升级或下线，并在边界封禁对它的直接访问。风险等级：严重。")),
    dict(id="rr02", cat="风险评级", answer=(
        "攻击者已从办公网段主机10.0.0.31获取域内两个普通账号，暂未发现对域控的访问记录，"
        "但该主机对所在/24网段发起了445端口扫描。考虑到终端口令复用率高且未部署LAPS，"
        "本地管理员口令大概率相同，横向扩散风险评级为高危。建议优先重置受损账号、"
        "启用LAPS并审计域控日志。风险等级：高危。")),
    dict(id="rr03", cat="风险评级", answer=(
        "已确认1.2万条客户记录（含姓名与手机号）被导出至外部FTP，导出行为由已失陷的应用服务器发起，"
        "持续了两周。数据不含支付信息，但属于个人信息，需评估是否触发监管上报义务。"
        "综合评级为严重，建议立即切断外联通道并启动合规上报流程。风险等级：严重。")),
    dict(id="rr04", cat="风险评级", answer=(
        "项目引用的log4j版本为2.14.1，受Log4Shell影响，但该组件仅在内部管理后台使用，"
        "入口不对互联网开放，且未发现jndi外联记录，利用前提受限，综合评级为中危。"
        "建议纳入下个迭代升级，并在出口防火墙上观察jndi外联特征。风险等级：中危。")),
    dict(id="rr05", cat="风险评级", answer=(
        "勒索事件处置后72小时复查：加密行为已停止，但攻击者创建的隐藏账户adm_backup仍然存在，"
        "计划任务中残留每小时执行的PowerShell脚本。判断攻击者仍保留持久化通道，评级为高危，"
        "建议删除该账户与任务、重置全部本地管理员口令后再次复查。风险等级：高危。")),

    # ---------------- 应急响应 ----------------
    dict(id="ir01", cat="应急响应", answer=(
        "凌晨2:14财务服务器文件被批量加密，后缀为.finmark，桌面留有勒索信README.txt。"
        "溯源发现攻击者经3389端口爆破进入（源IP 203.0.113.77），随后将数据外传至"
        "ftp.example-store.net。建议立即断网隔离，保留内存镜像与加密卷，"
        "排查同网段其他主机的3389暴露情况，暂不建议支付赎金。风险等级：严重。")),
    dict(id="ir02", cat="应急响应", answer=(
        "财务部5人收到仿冒发票邮件，附件为带宏的xlsm文件，其中1人已打开并输入了邮箱密码。"
        "建议立即重置该用户凭据并吊销其OAuth令牌，按主题与发件人特征召回其余邮件，"
        "并排查该账号在输入密码后有无异常登录记录。风险等级：中危。")),
    dict(id="ir03", cat="应急响应", answer=(
        "Web服务器被上传webshell并植入了nginx流量劫持模块。建议处置顺序为：先抓取内存与关键目录快照，"
        "再下线隔离，随后删除shell文件与劫持模块、重装nginx、轮换数据库与应用的全部凭据，"
        "最后从备份恢复数据并比对完整性。不宜跳过取证直接重装。风险等级：高危。")),
    dict(id="ir04", cat="应急响应", answer=(
        "研发终端在两周内向个人网盘上传约800MB源代码压缩包，发生在下班时段，共23次。"
        "该终端装有DLP但规则未覆盖源代码类型。建议约谈确认动机、封禁终端对网盘类站点的访问、"
        "补齐DLP规则，并评估泄露代码中是否包含生产环境凭据。风险等级：中危。")),
    dict(id="ir05", cat="应急响应", answer=(
        "对挖矿主机10.0.0.51的处置建议：先隔离网络防止任务下发，结束进程并删除其计划任务与"
        "注册表启动项，随后排查同网段主机的445端口异常会话（该样本具备MS17-010传播模块），"
        "最后修补该主机缺失的MS17-010补丁。风险等级：中危。")),
]

if __name__ == "__main__":
    assert len(TASKS) == 30, len(TASKS)
    cats = {}
    for t in TASKS:
        cats[t["cat"]] = cats.get(t["cat"], 0) + 1
    print("tasks:", len(TASKS), cats)
